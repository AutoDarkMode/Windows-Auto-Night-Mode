<Window
    x:Class="AutoDarkModeApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:AutoDarkModeApp.Helpers"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:lib="using:AutoDarkModeLib.Helpers"
    xmlns:local="using:AutoDarkModeApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TitleBar
            x:Name="TitleBar"
            Title="Auto Dark Mode"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            BackRequested="NavViewTitleBar_BackRequested"
            IsBackButtonEnabled="{x:Bind NavigationFrame.CanGoBack, Mode=OneWay}"
            IsBackButtonVisible="True"
            IsPaneToggleButtonVisible="True"
            PaneToggleRequested="NavViewTitleBar_PaneToggleRequested">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/AutoDarkModeIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <NavigationView
            x:Name="NavigationViewControl"
            Grid.Row="1"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            IsSettingsVisible="True"
            OpenPaneLength="240"
            PaneDisplayMode="Auto">
            <NavigationView.MenuItems>
                <!--
              TODO: Update item titles by updating <x:Uid>.Content entries in Strings/en-us/Resources.resw.
              https://docs.microsoft.com/windows/uwp/app-resources/localize-strings-ui-manifest#refer-to-a-string-resource-identifier-from-xaml

              TODO: Update item icons by updating FontIcon.Glyph properties.
              https://docs.microsoft.com/windows/apps/design/style/segoe-fluent-icons-font#icon-list
                -->
                <NavigationViewItem
                    Content="{lib:ResourceString Name=Time}"
                    Icon="{ui:FontIcon Glyph=&#xE823;}"
                    Tag="Time" />
                <NavigationViewItem
                    Content="{lib:ResourceString Name=SwitchModes}"
                    Icon="{ui:FontIcon Glyph=&#xE945;}"
                    Tag="SwitchModes" />
                <!--  TODO: Update item content in resource  -->
                <NavigationViewItem
                    Content="{lib:ResourceString Name=SystemAreas}"
                    Icon="{ui:FontIcon Glyph=&#xEC4E;}"
                    Tag="SystemAreas" />
                <NavigationViewItem
                    Content="{lib:ResourceString Name=Personalization}"
                    Icon="{ui:FontIcon Glyph=&#xE771;}"
                    Tag="Personalization" />
                <NavigationViewItem
                    Content="{lib:ResourceString Name=Scripts}"
                    Icon="{ui:FontIcon Glyph=&#xF000;}"
                    Tag="Scripts" />
            </NavigationView.MenuItems>
            <NavigationView.FooterMenuItems>
                <NavigationViewItem
                    Content="{lib:ResourceString Name=Donation}"
                    Icon="{ui:FontIcon Glyph=&#xEB51;}"
                    Tag="Donation" />
                <NavigationViewItem
                    Content="{lib:ResourceString Name=About}"
                    Icon="{ui:FontIcon Glyph=&#xE946;}"
                    Tag="About" />
            </NavigationView.FooterMenuItems>
            <Frame x:Name="NavigationFrame" />
        </NavigationView>
    </Grid>
</Window>
