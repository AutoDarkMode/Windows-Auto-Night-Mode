<Page
    x:Class="AutoDarkModeApp.Views.ThemePickerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:AutoDarkModeApp.Helpers"
    xmlns:lib="using:AutoDarkModeLib.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid x:Name="ContentArea" Margin="{StaticResource NavigationViewPageContentMargin}">
            <StackPanel Style="{StaticResource PageBaseStackPanelStyle}">

                <!--  Theme  -->
                <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="{lib:ResourceString Name=ThemeTextBlockHeader}" />
                <controls:SettingsExpander
                    Header="{lib:ResourceString Name=ThemeToggleSwichEnableTheme}"
                    HeaderIcon="{ui:FontIcon Glyph=&#xe771;}"
                    IsExpanded="True">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.ThemeSwitchEnabled, Mode=TwoWay}" />
                    <controls:SettingsExpander.Items>
                        <controls:SettingsCard ContentAlignment="Left">
                            <CheckBox Content="{lib:ResourceString Name=ThemePickerComboBoxKeepActive}" IsChecked="{x:Bind ViewModel.ThemeKeepActiveEnabled, Mode=TwoWay}" />
                        </controls:SettingsCard>
                    </controls:SettingsExpander.Items>
                </controls:SettingsExpander>

                <!--  Theme Tutorial Step  -->
                <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="{lib:ResourceString Name=ThemeHeaderCreate}" />
                <InfoBar
                    x:Name="ThemeTutorialFirstStepInfoBar"
                    IsClosable="False"
                    IsOpen="True"
                    Message="{lib:ResourceString Name=ThemeTutorialStep2}">
                    <InfoBar.ActionButton>
                        <HyperlinkButton Click="OpenWindowsThemePageHyperlinkButton_Click" Content="{lib:ResourceString Name=ThemeTutorialStep1}" />
                    </InfoBar.ActionButton>
                </InfoBar>
                <InfoBar
                    x:Name="ThemeTutorialSecondStepInfoBar"
                    IsClosable="False"
                    IsOpen="True"
                    Message="{lib:ResourceString Name=ThemeTutorialStep3}" />
                <InfoBar
                    x:Name="ThemeTutorialThirdStepInfoBar"
                    IsClosable="False"
                    IsOpen="True"
                    Message="{lib:ResourceString Name=ThemeTutorialStep4}" />

                <!--  Pick Theme  -->
                <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="{lib:ResourceString Name=ThemeHeaderPick}" />
                <controls:SettingsCard Header="{lib:ResourceString Name=ThemeLight}" HeaderIcon="{ui:FontIcon Glyph=&#xE706;}">
                    <ComboBox
                        x:Name="LightThemeComboBox"
                        MinWidth="120"
                        SelectedItem="{x:Bind ViewModel.SelectedLightTheme, Mode=TwoWay}" />
                </controls:SettingsCard>
                <controls:SettingsCard Header="{lib:ResourceString Name=ThemeDark}" HeaderIcon="{ui:FontIcon Glyph=&#xE708;}">
                    <ComboBox
                        x:Name="DarkThemeComboBox"
                        MinWidth="120"
                        SelectedItem="{x:Bind ViewModel.SelectedDarkTheme, Mode=TwoWay}" />
                </controls:SettingsCard>
                <controls:SettingsCard
                    ActionIcon="{ui:FontIcon Glyph=&#xE838;}"
                    Click="OpenThemeFolderSettingsCard_Click"
                    Header="{lib:ResourceString Name=OpenPath}"
                    IsClickEnabled="True" />

                <!--  Ignore  -->
                <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="{lib:ResourceString Name=ThemeHeaderIgnoreFlags}" />
                <InfoBar
                    IsClosable="False"
                    IsOpen="True"
                    Message="{lib:ResourceString Name=ThemeIgnoreDisabledMessage}"
                    Severity="Warning" />
                <controls:SettingsCard ContentAlignment="Left">
                    <CheckBox Content="{lib:ResourceString Name=ThemeCheckBoxIgnoreBackground}" IsChecked="{x:Bind ViewModel.IgnoreBackgroundEnabled, Mode=TwoWay}" />
                </controls:SettingsCard>
                <controls:SettingsCard ContentAlignment="Left">
                    <CheckBox Content="{lib:ResourceString Name=ThemeCheckBoxIgnoreCursor}" IsChecked="{x:Bind ViewModel.IgnoreCursorEnabled, Mode=TwoWay}" />
                </controls:SettingsCard>
                <controls:SettingsCard ContentAlignment="Left">
                    <CheckBox Content="{lib:ResourceString Name=ThemeCheckBoxIgnoreSound}" IsChecked="{x:Bind ViewModel.IgnoreSoundEnabled, Mode=TwoWay}" />
                </controls:SettingsCard>
                <controls:SettingsCard ContentAlignment="Left">
                    <CheckBox Content="{lib:ResourceString Name=ThemeCheckBoxIgnoreDesktopIcons}" IsChecked="{x:Bind ViewModel.IgnoreDesktopIconsEnabled, Mode=TwoWay}" />
                </controls:SettingsCard>

            </StackPanel>
        </Grid>
    </ScrollViewer>

</Page>
