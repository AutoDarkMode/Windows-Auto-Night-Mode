<Page
    x:Class="AutoDarkModeApp.Views.WallpaperPickerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:AutoDarkModeApp.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">
    <Page.Resources>
        <helpers:FlagsToVisibilityConverter x:Key="FlagsToVisibilityConverter" />
        <helpers:EnumToIndexConverter x:Key="EnumToIndexConverter" />
        <helpers:EnumToStretchConverter x:Key="EnumToStretchConverter" />
    </Page.Resources>

    <ScrollViewer>
        <Grid x:Name="ContentArea" Margin="{StaticResource NavigationViewPageContentMargin}">
            <StackPanel Style="{StaticResource PageBaseStackPanelStyle}">
                <!--
                    TODO:
                    * Hide Border when setting == Spotlight?
                    * Hide DesktopPreviewImage when setting == Solid Color
                    * Stretch = ViewModel.SelectWallpaperFillingMode
                    * Get system display size (aspect ratio) and apply it to the image (width) ?
                -->
                <Border
                    Margin="0,0,0,12"
                    HorizontalAlignment="Left"
                    Background="{x:Bind ViewModel.ColorPreviewBorderBackground, Mode=OneWay}"
                    BorderBrush="Black"
                    BorderThickness="8"
                    CornerRadius="{StaticResource OverlayCornerRadius}">
                    <Image
                        x:Name="DesktopPreviewImage"
                        Width="320"
                        Height="180"
                        Source="{x:Bind ViewModel.GlobalWallpaperSource, Mode=OneWay}"
                        Stretch="{x:Bind ViewModel.SelectWallpaperFillingMode, Converter={StaticResource EnumToStretchConverter}, Mode=OneWay}"
                        ToolTipService.ToolTip="{x:Bind ViewModel.GlobalWallpaperPath, Mode=OneWay}" />
                </Border>

                <!--  Desktop background  -->
                <controls:SettingsCard Header="{helpers:ResourceString Name=WallpaperToggleSwitch}">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.IsWallpaperSwitchEnabled, Mode=TwoWay}" />
                </controls:SettingsCard>

                <controls:SettingsExpander
                    Header="{helpers:ResourceString Name=WallpaperComboBoxMode}"
                    HeaderIcon="{ui:FontIcon Glyph=&#xE790;}"
                    IsExpanded="True"
                    Visibility="{x:Bind ViewModel.IsWallpaperSwitchEnabled, Mode=OneWay}">

                    <!--  Wallpaper Switch Theme  -->
                    <ComboBox SelectedIndex="{x:Bind ViewModel.SelectWallpaperThemeMode, Converter={StaticResource EnumToIndexConverter}, Mode=TwoWay}">
                        <ComboBoxItem Content="{helpers:ResourceString Name=ThemeLight}" />
                        <ComboBoxItem Content="{helpers:ResourceString Name=ThemeDark}" />
                    </ComboBox>

                    <controls:SettingsExpander.Items>
                        <!--  Background Type  -->
                        <controls:SettingsCard ContentAlignment="Left">
                            <RadioButtons SelectedIndex="{x:Bind ViewModel.CurrentDisplayMode, Converter={StaticResource EnumToIndexConverter}, Mode=TwoWay}">
                                <RadioButton Content="{helpers:ResourceString Name=WallpaperComboBoxItemPicture}" />
                                <RadioButton Content="{helpers:ResourceString Name=WallpaperComboBoxItemPictureMM}" />
                                <RadioButton Content="{helpers:ResourceString Name=WallpaperComboBoxItemSolidColor}" />
                                <RadioButton Content="{helpers:ResourceString Name=WallpaperComboBoxItemSpotlight}" />
                            </RadioButtons>
                        </controls:SettingsCard>

                        <!--  Picture  -->
                        <controls:SettingsCard Header="{helpers:ResourceString Name=WallpaperTextBlockChooseWallpaper}" Visibility="{x:Bind ViewModel.CurrentDisplayFlags, Converter={StaticResource FlagsToVisibilityConverter}, ConverterParameter=ShowImageSettings, Mode=OneWay}">
                            <Button
                                MinWidth="120"
                                HorizontalAlignment="Right"
                                Command="{x:Bind ViewModel.PickFileCommand}"
                                Content="{helpers:ResourceString Name=dbFilePicker}" />
                        </controls:SettingsCard>

                        <!--  Picture fit  -->
                        <controls:SettingsCard Header="{helpers:ResourceString Name=WallpaperTextBlockPosition}" Visibility="{x:Bind ViewModel.CurrentDisplayFlags, Converter={StaticResource FlagsToVisibilityConverter}, ConverterParameter=ShowFillingWaySettings, Mode=OneWay}">
                            <ComboBox SelectedIndex="{x:Bind ViewModel.SelectWallpaperFillingMode, Converter={StaticResource EnumToIndexConverter}, Mode=TwoWay}">
                                <ComboBoxItem Content="Center" />
                                <ComboBoxItem Content="{helpers:ResourceString Name=WallpaperComboBoxItemStretch}" />
                                <ComboBoxItem Content="{helpers:ResourceString Name=WallpaperComboBoxItemFit}" />
                                <ComboBoxItem Content="{helpers:ResourceString Name=WallpaperComboBoxItemFill}" />
                            </ComboBox>
                        </controls:SettingsCard>

                        <!--  Monitor  -->
                        <controls:SettingsCard Header="{helpers:ResourceString Name=WallpaperComboBoxMonitor}" Visibility="{x:Bind ViewModel.CurrentDisplayFlags, Converter={StaticResource FlagsToVisibilityConverter}, ConverterParameter=ShowMonitorSettings, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <HyperlinkButton Click="RemoveDisconnectedMonitorsHyperlinkButton_Click" Content="{helpers:ResourceString Name=WallpaperHyperLinkRemoveDisconnected}" />
                                <ComboBox
                                    x:Name="MonitorsComboBox"
                                    HorizontalAlignment="Right"
                                    SelectedItem="{x:Bind ViewModel.SelectMonitor, Mode=TwoWay}" />
                            </StackPanel>
                        </controls:SettingsCard>

                        <!--  Color  -->
                        <controls:SettingsCard Header="{helpers:ResourceString Name=WallpaperColor}" Visibility="{x:Bind ViewModel.CurrentDisplayFlags, Converter={StaticResource FlagsToVisibilityConverter}, ConverterParameter=ShowColorSettings, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <!--  TODO: Replace Content to Resource  -->
                                <Button
                                    MinWidth="120"
                                    Click="CheckColorButton_Click"
                                    Content="Select color" />
                            </StackPanel>
                        </controls:SettingsCard>

                        <!--  Windows Spotlight  -->
                        <controls:SettingsCard ContentAlignment="Left" Visibility="{x:Bind ViewModel.CurrentDisplayFlags, Converter={StaticResource FlagsToVisibilityConverter}, ConverterParameter=ShowSpotlight, Mode=OneWay}">
                            <HyperlinkButton Click="WindowsSpotlightHyperlinkButton_Click" Content="{helpers:ResourceString Name=WallpaperComboBoxItemSpotlight}" />
                        </controls:SettingsCard>
                    </controls:SettingsExpander.Items>
                </controls:SettingsExpander>


                <!--  TODO: follow https://learn.microsoft.com/windows/apps/design/controls/dialogs-and-flyouts/dialogs  -->
                <!--  Color Picker Dialog  -->
                <ContentDialog
                    x:Name="ColorPickerContentDialog"
                    CloseButtonText="{helpers:ResourceString Name=cancel}"
                    DefaultButton="Primary"
                    PrimaryButtonText="{helpers:ResourceString Name=lblSet}">
                    <StackPanel>
                        <ColorPicker x:Name="CheckColorColorPicker" />
                    </StackPanel>
                </ContentDialog>

            </StackPanel>
        </Grid>
    </ScrollViewer>

</Page>
