<Page
    x:Class="AutoDarkModeApp.Views.WallpaperPickerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:AutoDarkModeApp.Helpers"
    xmlns:lib="using:AutoDarkModeLib.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">
    <Page.Resources>
        <helpers:FlagsToVisibilityConverter x:Key="FlagsToVisibilityConverter" />
        <helpers:EnumToIndexConverter x:Key="EnumToIndexConverter" />
        <helpers:EnumToStretchConverter x:Key="EnumToStretchConverter" />
    </Page.Resources>

    <ScrollViewer>
        <Grid x:Name="ContentArea" Margin="{StaticResource NavigationViewPageContentMargin}">
            <StackPanel Style="{StaticResource PageBaseStackPanelStyle}">
                <!--
                    TODO:
                    * Hide Border when setting == Spotlight?
                    * Hide DesktopPreviewImage when setting == Solid Color
                    * Stretch = ViewModel.SelectWallpaperFillingMode
                    * Get system display size (aspect ratio) and apply it to the border (width) ?
                -->
                <Border
                    Margin="0,0,0,12"
                    HorizontalAlignment="Left"
                    Background="{x:Bind ViewModel.ColorPreviewBorderBackground, Mode=OneWay}"
                    BorderBrush="Black"
                    BorderThickness="8"
                    CornerRadius="{StaticResource OverlayCornerRadius}">
                    <Image
                        x:Name="DesktopPreviewImage"
                        Width="320"
                        Height="180"
                        Source="{x:Bind ViewModel.GlobalWallpaperSource, Mode=OneWay}"
                        Stretch="{x:Bind ViewModel.SelectWallpaperFillingMode, Converter={StaticResource EnumToStretchConverter}, Mode=OneWay}"
                        ToolTipService.ToolTip="{x:Bind ViewModel.GlobalWallpaperPath, Mode=OneWay}" />
                </Border>

                <!--  Desktop background  -->
                <controls:SettingsCard Header="{lib:ResourceString Name=EnableWallpaperSwitch}">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.IsWallpaperSwitchEnabled, Mode=TwoWay}" />
                </controls:SettingsCard>

                <controls:SettingsExpander
                    Header="{lib:ResourceString Name=PickTheme}"
                    HeaderIcon="{ui:FontIcon Glyph=&#xE790;}"
                    IsExpanded="True"
                    Visibility="{x:Bind ViewModel.IsWallpaperSwitchEnabled, Mode=OneWay}">

                    <!--  Wallpaper Switch Theme  -->
                    <ComboBox SelectedIndex="{x:Bind ViewModel.SelectWallpaperThemeMode, Converter={StaticResource EnumToIndexConverter}, Mode=TwoWay}">
                        <ComboBoxItem Content="{lib:ResourceString Name=LightTheme}" />
                        <ComboBoxItem Content="{lib:ResourceString Name=DarkTheme}" />
                    </ComboBox>

                    <controls:SettingsExpander.Items>
                        <!--  Background Type  -->
                        <controls:SettingsCard ContentAlignment="Left">
                            <RadioButtons SelectedIndex="{x:Bind ViewModel.CurrentDisplayMode, Converter={StaticResource EnumToIndexConverter}, Mode=TwoWay}">
                                <RadioButton Content="{lib:ResourceString Name=WallpaperMode_Picture}" />
                                <RadioButton Content="{lib:ResourceString Name=WallpaperMode_PictureMM}" />
                                <RadioButton Content="{lib:ResourceString Name=WallpaperMode_SolidColor}" />
                                <RadioButton Content="{lib:ResourceString Name=WallpaperMode_Spotlight}" />
                            </RadioButtons>
                        </controls:SettingsCard>

                        <!--  Picture  -->
                        <controls:SettingsCard Header="{lib:ResourceString Name=ChooseWallpaper}" Visibility="{x:Bind ViewModel.CurrentDisplayFlags, Converter={StaticResource FlagsToVisibilityConverter}, ConverterParameter=ShowImageSettings, Mode=OneWay}">
                            <Button
                                MinWidth="120"
                                HorizontalAlignment="Right"
                                Command="{x:Bind ViewModel.PickFileCommand}"
                                Content="{lib:ResourceString Name=BrowsePhotos}" />
                        </controls:SettingsCard>

                        <!--  Picture fit  -->
                        <controls:SettingsCard Header="{lib:ResourceString Name=Position}" Visibility="{x:Bind ViewModel.CurrentDisplayFlags, Converter={StaticResource FlagsToVisibilityConverter}, ConverterParameter=ShowFillingWaySettings, Mode=OneWay}">
                            <ComboBox SelectedIndex="{x:Bind ViewModel.SelectWallpaperFillingMode, Converter={StaticResource EnumToIndexConverter}, Mode=TwoWay}">
                                <ComboBoxItem Content="Center" />
                                <ComboBoxItem Content="{lib:ResourceString Name=FitMode_Stretch}" />
                                <ComboBoxItem Content="{lib:ResourceString Name=FitMode_Fit}" />
                                <ComboBoxItem Content="{lib:ResourceString Name=FitMode_Fill}" />
                            </ComboBox>
                        </controls:SettingsCard>

                        <!--  Monitor  -->
                        <controls:SettingsCard Header="{lib:ResourceString Name=Monitor}" Visibility="{x:Bind ViewModel.CurrentDisplayFlags, Converter={StaticResource FlagsToVisibilityConverter}, ConverterParameter=ShowMonitorSettings, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <HyperlinkButton Click="RemoveDisconnectedMonitorsHyperlinkButton_Click" Content="{lib:ResourceString Name=RemoveDisconnected}" />
                                <ComboBox
                                    x:Name="MonitorsComboBox"
                                    HorizontalAlignment="Right"
                                    SelectedItem="{x:Bind ViewModel.SelectMonitor, Mode=TwoWay}" />
                            </StackPanel>
                        </controls:SettingsCard>

                        <!--  Color  -->
                        <controls:SettingsCard Header="{lib:ResourceString Name=Color}" Visibility="{x:Bind ViewModel.CurrentDisplayFlags, Converter={StaticResource FlagsToVisibilityConverter}, ConverterParameter=ShowColorSettings, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <!--  TODO: Replace Content to Resource  -->
                                <Button
                                    MinWidth="120"
                                    Click="CheckColorButton_Click"
                                    Content="Select color" />
                            </StackPanel>
                        </controls:SettingsCard>

                        <!--  Windows Spotlight  -->
                        <controls:SettingsCard ContentAlignment="Left" Visibility="{x:Bind ViewModel.CurrentDisplayFlags, Converter={StaticResource FlagsToVisibilityConverter}, ConverterParameter=ShowSpotlight, Mode=OneWay}">
                            <HyperlinkButton Click="WindowsSpotlightHyperlinkButton_Click" Content="{lib:ResourceString Name=OpenWindowsSpotlight}" />
                        </controls:SettingsCard>
                    </controls:SettingsExpander.Items>
                </controls:SettingsExpander>


                <!--  TODO: follow https://learn.microsoft.com/windows/apps/design/controls/dialogs-and-flyouts/dialogs  -->
                <!--  Color Picker Dialog  -->
                <ContentDialog
                    x:Name="ColorPickerContentDialog"
                    CloseButtonText="{lib:ResourceString Name=Cancel}"
                    DefaultButton="Primary"
                    PrimaryButtonText="{lib:ResourceString Name=Set}">
                    <StackPanel>
                        <ColorPicker x:Name="CheckColorColorPicker" />
                    </StackPanel>
                </ContentDialog>

            </StackPanel>
        </Grid>
    </ScrollViewer>

</Page>
