<Page
    x:Class="AutoDarkModeApp.Views.ColorizationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:AutoDarkModeApp.Helpers"
    xmlns:lib="using:AutoDarkModeLib.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">
    <Page.Resources>
        <helpers:EnumToIndexConverter x:Key="EnumToIndexConverter" />
    </Page.Resources>

    <ScrollViewer>
        <Grid x:Name="ContentArea" Margin="{StaticResource NavigationViewPageContentMargin}">
            <StackPanel Style="{StaticResource PageBaseStackPanelStyle}">

                <!--  Colorization  -->
                <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="{lib:ResourceString Name=ColorizationPick_Description
}" />
                <controls:SettingsCard Header="{lib:ResourceString Name=EnableAccentColorSwitch}" HeaderIcon="{ui:FontIcon Glyph=&#xE790;}">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.IsColorizationEnabled, Mode=TwoWay}" />
                </controls:SettingsCard>

                <!--  Light  -->
                <controls:SettingsExpander
                    Header="{lib:ResourceString Name=LightTheme}"
                    HeaderIcon="{ui:FontIcon Glyph=&#xE706;}"
                    IsExpanded="True">
                    <ComboBox SelectedIndex="{x:Bind ViewModel.LightThemeMode, Mode=TwoWay, Converter={StaticResource EnumToIndexConverter}}">
                        <ComboBoxItem Content="{lib:ResourceString Name=Automatic}" />
                        <ComboBoxItem Content="{lib:ResourceString Name=Manual}" />
                    </ComboBox>
                    <controls:SettingsExpander.Items>
                        <controls:SettingsCard Header="{lib:ResourceString Name=PickAccentColor}" IsEnabled="{x:Bind ViewModel.IsLightThemeSettingsCardEnabled, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal" Spacing="12">
                                <Border
                                    Width="30"
                                    Height="28"
                                    Background="{x:Bind ViewModel.LightModeColorPreviewBorderBackground, Mode=OneWay}"
                                    CornerRadius="{StaticResource ControlCornerRadius}" />
                                <!--  TODO: Replace Content to Res  -->
                                <Button
                                    MinWidth="120"
                                    Click="LightModeCheckColorButton_Click"
                                    Content="Select color" />
                            </StackPanel>
                        </controls:SettingsCard>
                    </controls:SettingsExpander.Items>
                </controls:SettingsExpander>

                <!--  Dark  -->
                <controls:SettingsExpander Header="{lib:ResourceString Name=DarkTheme}" HeaderIcon="{ui:FontIcon Glyph=&#xE793;}">
                    <ComboBox SelectedIndex="{x:Bind ViewModel.DarkThemeMode, Mode=TwoWay, Converter={StaticResource EnumToIndexConverter}}">
                        <ComboBoxItem Content="{lib:ResourceString Name=Automatic}" />
                        <ComboBoxItem Content="{lib:ResourceString Name=Manual}" />
                    </ComboBox>
                    <controls:SettingsExpander.Items>
                        <controls:SettingsCard Header="{lib:ResourceString Name=PickAccentColor}" IsEnabled="{x:Bind ViewModel.IsDarkThemeSettingsCardEnabled, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal" Spacing="12">
                                <Border
                                    Width="30"
                                    Height="28"
                                    Background="{x:Bind ViewModel.DarkModeColorPreviewBorderBackground, Mode=OneWay}"
                                    CornerRadius="{StaticResource ControlCornerRadius}" />
                                <!--  TODO: Replace Content to Res  -->
                                <Button
                                    MinWidth="120"
                                    Click="DarkModeCheckColorButton_Click"
                                    Content="Select color" />
                            </StackPanel>
                        </controls:SettingsCard>
                    </controls:SettingsExpander.Items>
                </controls:SettingsExpander>

                <!--  TODO: follow https://learn.microsoft.com/windows/apps/design/controls/dialogs-and-flyouts/dialogs  -->
                <!--  Color Picker Dialog  -->
                <ContentDialog
                    x:Name="ColorPickerContentDialog"
                    CloseButtonText="{lib:ResourceString Name=cancel}"
                    DefaultButton="Primary"
                    PrimaryButtonText="{lib:ResourceString Name=lblSet}">
                    <StackPanel>
                        <ColorPicker x:Name="CheckColorColorPicker" />
                    </StackPanel>
                </ContentDialog>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
