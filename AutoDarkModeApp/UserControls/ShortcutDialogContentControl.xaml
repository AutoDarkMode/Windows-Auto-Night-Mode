<UserControl
    x:Class="AutoDarkModeApp.UserControls.ShortcutDialogContentControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:AutoDarkModeApp.Helpers"
    xmlns:local="using:AutoDarkModeApp.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel
        Orientation="Vertical"
        PreviewKeyDown="StackPanel_PreviewKeyDown"
        Spacing="16">
        <TextBlock Text="{helpers:ResourceString Name=SelectTheKeyCombination}" />
        <ItemsControl
            Height="56"
            Margin="0,24,0,24"
            HorizontalAlignment="Center"
            ItemsSource="{x:Bind Keys, Mode=OneWay}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="4" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="local:SingleHotkeyDataObject">
                    <Grid>
                        <Border
                            Height="48"
                            MinWidth="48"
                            Background="{ThemeResource AccentFillColorDefaultBrush}"
                            CornerRadius="{StaticResource ControlCornerRadius}">
                            <ContentPresenter
                                Padding="4"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Content="{x:Bind Key}"
                                FontSize="18"
                                FontWeight="SemiBold"
                                Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                        </Border>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <TextBlock Style="{StaticResource CaptionTextBlockStyle}" Text="{helpers:ResourceString Name=HotkeyEditHint}" />
    </StackPanel>

</UserControl>
